#define _CRT_SECURE_NO_DEPRECATE

/*
    Stampa di numeri reali con "dettaglio" a piacimento
    Si realizzi una funzione stampaDettagli(...) che riceva come parametri un numero reale e due interi
    (che indicano, rispettivamente, il numero di cifre per la parte intera e per la parte decimale)
    La funzione stampi a video il numero secondo le indicazioni ricevute come parametri
    Come? Tramite una opportuna stringa di formato (es. "%6.2f" significa stampare un float con 6 cifre per
    la parte intera e due per la parte decimale)
    La funzione componga dinamicamente una opportuna stringa di formato, utilizzando la funzione sprintf(...) ed
    una stringa allocata staticamente (si controlli di non eccedere la dimensione della stringa già allocata)
*/

#include <stdio.h>

void stampaDettagli(double a, int b, int c);

int main() {



    return 0;

}

void stampaDettagli(double a, int b, int c) {

}